// Kept as a compatibility entrypoint for the former settings-only migration.
// The real clinic migration now syncs settings, pages, locations, treatments,
// conditions, and the founder profile together so stale mock data cannot return.
import './sync-real-clinic-data';
